# Pinned to exact versions. Verified twice: installing and running the
# full pipeline + test suite directly in a Python 3.10.12 sandbox, and
# again via `docker build` on python:3.11-slim (see Dockerfile) -- both
# environments pass all 6 checks in test_pipeline.py and run_pipeline.py
# end to end.
# Previously left as >=; README flagged unpinned deps as an open
# reproducibility limitation.
# Previously left as >=; README flagged unpinned deps as an open
# reproducibility limitation.
faker==40.36.0
pandas==2.3.3
numpy==2.2.6
matplotlib==3.10.9
pyyaml==6.0.3
